% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geoRflow_raster_pipeline_point.R
\name{geoRflow_raster_pipeline_point}
\alias{geoRflow_raster_pipeline_point}
\title{Process Geospatial Raster Data and Extract Point Values}
\usage{
geoRflow_raster_pipeline_point(
  inputs,
  df = NULL,
  lat_col = NULL,
  lon_col = NULL,
  split_id = NULL,
  search_strings = NULL,
  method = "stars",
  resample_factor = NULL,
  crs = st_crs(4326),
  method_resampling = "bilinear",
  no_data_value = -9999,
  reference_shape = NULL,
  use_bilinear = TRUE
)
}
\arguments{
\item{inputs}{A list of file paths or raster objects to be processed.}

\item{df}{A data frame containing point data (default is NULL).}

\item{lat_col}{The name of the column in `df` that contains latitude values (default is NULL).}

\item{lon_col}{The name of the column in `df` that contains longitude values (default is NULL).}

\item{split_id}{The name of the column in `df` to split the data frame on for processing (default is NULL).}

\item{search_strings}{A list of strings for filtering files (default is NULL, not currently used in function).}

\item{method}{The method for loading and processing raster data, either "stars" or "terra" (default is "stars").}

\item{resample_factor}{A numeric value to rescale the raster data (default is NULL).}

\item{crs}{The coordinate reference system to be used for the raster data (default is st_crs(4326)).}

\item{method_resampling}{The method used for resampling when changing raster resolution (default is "bilinear").}

\item{no_data_value}{The value to be used for missing data in the raster (default is -9999).}

\item{reference_shape}{A file path or spatial object for cropping the raster (default is NULL).}

\item{use_bilinear}{Logical, whether to use bilinear interpolation when extracting raster values (default is TRUE).}
}
\value{
A list with two elements: `processed_rasters`, a list of processed raster objects,
and `dataframes_with_values`, a list of data frames with extracted values from the raster data.
}
\description{
This function processes a list of geospatial raster data files or objects, resamples them,
reprojects them to a specified CRS, and extracts values at specified point locations from a data frame.
}
\examples{
\donttest{
# Replace the following paths with the actual paths to your raster files:
# "path/to/raster1.tif" and "path/to/raster2.tif".
raster_files <- c("path/to/raster1.tif", "path/to/raster2.tif")
# Replace the coordinates in points_df with the actual coordinates.
points_df <- data.frame(lon = c(-120, -121), lat = c(38, 39))

# This is how you would call the function with your own data:
# results <- geoRflow_raster_pipeline_point(inputs = raster_files,
#                                           df = points_df,
#                                           lat_col = "lat",
#                                           lon_col = "lon")

# To view the processed rasters and extracted values:
# processed_rasters <- results$processed_rasters
# extracted_values_df <- results$dataframes_with_values[[1]]
}
}
